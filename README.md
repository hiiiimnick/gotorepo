# gotorepo

Small utility to open the repository from the current directory in the browser

## Installation

Clone Repository, enter the directory and run 
```
cargo install --path .
```


## Usage

run in a directory of a git repository. 
